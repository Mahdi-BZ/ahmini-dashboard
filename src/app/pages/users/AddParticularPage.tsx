import * as React from 'react';
import ParticularForm from './ParticularForm';

interface IAddParticularPageProps {
}

const AddParticularPage: React.FunctionComponent<IAddParticularPageProps> = (props) => {
  return (
      <ParticularForm />
  );
};

export default AddParticularPage;
