import * as React from 'react';
import SponsorshipRequestForm from './SponsorshipRequestForm';

interface IAppProps {
}

const App: React.FunctionComponent<IAppProps> = (props) => {
  return (<SponsorshipRequestForm />);
};

export default App;
